# components/

Componentes de UI reutilizables (presentacionales): `TodoForm`, `TodoList`,
`TodoItem`, botones, inputs. No deben contener lógica de negocio ni llamadas
a servicios externos directamente (eso vive en `features/` y `services/`).
Se completan a partir del Hito 6 (CRUD de tareas).
