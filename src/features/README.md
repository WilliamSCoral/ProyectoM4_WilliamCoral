# features/

Lógica de negocio organizada por dominio (ej: `auth/`, `tasks/`). Aquí vive,
por ejemplo, el componente `Authenticator` (Context + hook `useAuth`) del
Hito 3, y la lógica de orquestación del CRUD de tareas del Hito 6.
