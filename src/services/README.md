# services/

Integraciones con servicios externos: inicialización de Firebase
(`firebase.ts`), funciones de Firestore (`taskService.ts`), llamadas a la
API de emails (`emailService.ts`). No deben contener JSX ni lógica de UI.
Se completan a partir del Hito 2 (Configuración de Firebase).
