# hooks/

Custom hooks reutilizables: `useAuth`, `useTasks`. Encapsulan estado,
efectos y la comunicación con `services/` para que los componentes no
llamen directamente a Firebase. Se completan en los Hitos 3 y 6.
