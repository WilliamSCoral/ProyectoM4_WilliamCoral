# routes/

Configuración de React Router y el componente `ProtectedRoute` que decide
si el usuario autenticado puede acceder a una ruta o debe ser redirigido.
Se completa en el Hito 4 (Rutas protegidas).
