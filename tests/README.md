# tests/

Tests unitarios y de componentes con Vitest + React Testing Library.
Incluye mocks de servicios externos (Firebase Auth, Firestore, el
endpoint de envío de emails) para no depender de red real durante los
tests. Se completa en el Hito 8 (Testing).
